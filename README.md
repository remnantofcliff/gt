# gt - get_time - a simple c library to get the current high resolution time 

Provides the ´get_time´-function to get the current high resolution time in
seconds. Should work on Linux / Mac / Unix / Windows.